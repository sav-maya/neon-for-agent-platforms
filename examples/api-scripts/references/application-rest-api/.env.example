# Optional vars for references/application-rest-api/curl-examples.sh
# Never commit real cookies or tokens.

APP_BASE_URL=http://localhost:3000

# UUIDs from your app after creating a project / version (placeholders for local dev)
APP_PROJECT_ID=
APP_VERSION_ID=

# After logging into your local app: DevTools → Network → copy Cookie header value
# STACK_SESSION_COOKIE=
